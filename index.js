require('@entkenntnis/challenges-server')(config => {
  // config.theme = 'yeti'
  // config.locale = 'de
  // config.brand = 'My-Server'
  // config.slogan = 'Learning by challenges!'
  /* config.i18nExtend.push({
    lng: 'de',
    key: 'contact.content_',
    value: '<h1>Kontaktinformationen</h1>'
  }) */
  /* config.i18nExtend.push({
    lng: 'de',
    key: 'privacy.content_',
    value: '<h1>Datenschutzerklärung</h1>'
  }) */
  return config
})
